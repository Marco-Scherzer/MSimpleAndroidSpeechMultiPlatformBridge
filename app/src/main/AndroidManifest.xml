<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.marcoscherzer.msimplespeechbackend">

    <!-- Mikrofonberechtigung für Spracherkennung -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Netzwerkberechtigung für lokalen HTTP-Server -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Erlaubt Empfang von Media-Button-Events (Headset, Bluetooth, Auto) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Msimplespeechbackend">

        <!-- Start-Activity -->
        <activity
            android:name="com.marcoscherzer.msimplespeechbackend.app.MMain"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Media-Button Receiver (Headset-Hook, Bluetooth-Buttons, Auto-Freisprecheinrichtung) -->
        <receiver
            android:name="com.marcoscherzer.msimplespeechbackend.app.MMediaButtonReceiver"
            android:exported="true">

            <!-- Priorität hochsetzen, damit App zuerst den Button bekommt -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>

        </receiver>

    </application>

</manifest>

